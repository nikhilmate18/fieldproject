DB_CONFIG = {
    "host": "localhost",
    "user": "root",          # change if your MySQL user is different
    "password": "admin",          # set your MySQL password
    "database": "document_db"  # this DB will be created via schema.sql
}

SECRET_KEY = "change-this-secret-key"  # replace with any random string
